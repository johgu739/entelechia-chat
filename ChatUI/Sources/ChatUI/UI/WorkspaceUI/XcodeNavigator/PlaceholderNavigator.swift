import SwiftUI

struct PlaceholderNavigator: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Navigator not implemented yet")
                .font(.system(size: 12))
                .foregroundColor(.secondary)
            Spacer()
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

